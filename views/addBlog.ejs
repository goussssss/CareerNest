<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>    
    <title>Add new Experience | CareerNest</title>
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="container mt-5">
        <div class="row justify-content-center">
          <div class="col-md-8">     
            <div class="card">
              <div class="card-header bg-primary text-white">
                <h3 class="mb-0">Create New Career Post</h3>
              </div>
              <div class="card-body">
                <form action="/blog/" method="post" enctype="multipart/form-data">
                  <!-- Title field -->
                  <div class="mb-3">
                    <label for="title" class="form-label">Title</label>
                    <input type="text" class="form-control" id="title" name="title" aria-describedby="title" required>
                    <div class="form-text">Give your post a descriptive title</div>
                  </div>
                  
                  <!-- Body field -->
                  <div class="mb-3">
                    <label for="body" class="form-label">Body</label>
                    <textarea class="form-control" id="body" name="body" rows="10" required></textarea>
                    <div class="form-text">Share your career experience and important tips here</div>
                  </div>
                  
                  <!-- Cover Image field -->
                  <div class="mb-3">
                    <label for="coverImage" class="form-label">Cover Image</label>
                    <input class="form-control" type="file" id="coverImage" name="coverImage" aria-describedby="coverImage">
                    <div class="form-text">Upload a cover image for your post (optional)</div>
                  </div>
                  
                  <!-- PDF file field -->
                  <div class="mb-3">
                    <label for="pdfFile" class="form-label">PDF Document</label>
                    <input class="form-control" type="file" id="pdfFile" name="pdfFile" aria-describedby="pdfFile" accept="application/pdf">
                    <div class="form-text">Upload a PDF document with additional information (optional)</div>
                  </div>
                  
                  <!-- Submit button -->
                  <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Publish Post</button>
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>

    <%- include('./partials/scripts') %>  
</body>  
</html>